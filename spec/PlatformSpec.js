describe("platform", () => {
    var platform = require('../src/platform');

    it("platform should be a function", function() {
        expect(typeof platform).toBe("object");
    });
    it("应该添加一个文件夹，即Cordova项目", function() {
        expect(typeof platform).toBe("object");
    });
});
