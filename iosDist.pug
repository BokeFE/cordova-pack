doctype html
html(lang="en")
  head
    meta(charset="utf-8")
    meta(name="format-detection" content="telephone=no")
    meta(name="msapplication-tap-highlight" content="no")
    meta(name="viewport" content="user-scalable=no, initial-scale=1, maximum-scale=1, minimum-scale=1, width=device-width, height=device-height")
    title 下载#{appName}
    script.
      function isWeixinBrowser(){ return /micromessenger/i.test(navigator.userAgent); }
      window.onload = function(){
        var ios = document.getElementsByClassName('ios')[0];
        ios.addEventListener('click',function(){
          if(isWeixinBrowser()){
            alert('请使用Safari浏览器打开此页面');
          }
        });
      };
    style.
      body{
        background-color:#F9FAFF;
      }
      h1{
      text-align:center;
      }
      .ios{
           height: 80px;
           line-height: 80px;
           width: 100%;
           display: block;
           background-color: #3475B7;
           color: #ffffff;
           font-size: 2em;
           text-decoration: none;
           border-radius: 2px;
           text-align: center;
           vertical-align: middle;
           position: absolute;
           top: 50%;
           margin-top: -50px;
           left: 50%;
           margin-left: -25%;
           width: 50%;
           }


  body
    h1 #{appName}
    div
        a(class="ios" href='itms-services://?action=download-manifest&url=' + plistUrl) iOS下载