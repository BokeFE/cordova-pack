doctype html
html(lang="en")
  head
    title Example
    script.
      if (foo) {
        bar();
      }
  body
    h1 Pug - node template engine
    div
        a(class="ios" href='itms-services://?action=download-manifest&url=' + iosUrl) iOS下载